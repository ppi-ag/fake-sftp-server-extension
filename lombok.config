# tell lombok that this is the root-config (so lombok will not search for more config-files in parent-folders)
config.stopbubbling = true

# return objects on setter (instead of void)
lombok.accessors.chain=true
